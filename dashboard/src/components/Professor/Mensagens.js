import react from 'react';
import api from '../../utils/api';

export const Mensagem = props =>{
    return(
        <main class='container'>
            <h1>Mande uma mensagem pra gente!</h1>    
        </main>
    )
}